# План разработки «Планировщика приёмов и увольнений»

План составлен по `tz.md`. Работа разбита на пять последовательных этапов. Каждый этап заканчивается готовым, проверяемым результатом. Автоматические проверки выполняет разработчик; пользователю передаются только проверки внешнего вида и удобства, которые нельзя достоверно оценить автоматически.

**Статус разработки:** все 5 шагов выполнены.

## Шаг 1. Каркас интерфейса и базовое состояние приложения — ✅ выполнен

### Работа

- Создать предусмотренные ТЗ файлы `index.html`, `style.css` и `script.js` и корректно связать их.
- Собрать семантическую структуру основного экрана: шапку, две кнопки создания событий, выбор периода, сводные карточки, календарь, таблицу, контейнер уведомлений и корень модальных окон.
- Добавить заданные справочники подразделений и должностей, перечни типов событий и допустимых статусов.
- Описать состояние приложения и модель записи в соответствии с ТЗ.
- Реализовать русское форматирование дат и ставки, вычисление текущей недели, сортировку событий и фильтрацию по периоду и типу.
- Реализовать чтение и сохранение записей и настроек в `localStorage`, включая первый запуск, восстановление выбранного периода и активного фильтра.
- Подключить месячный календарь через CDN и предусмотреть понятный резервный режим, при котором таблица продолжает работать, если календарь не загрузился.
- Оформить основной тёмный минималистичный интерфейс в чёрной, белой и мятной палитре.

### Готовый результат

Открывается полноценный основной экран без демонстрационных данных. При первом запуске выбран период текущей недели, счётчики равны нулю, календарь пуст, заголовки таблицы видны, настройки сохраняются после перезагрузки. Интерфейс имеет базовое адаптивное оформление.

### Автоматическая проверка

- Проверить, что существуют ровно три требуемых прикладных файла и пути их подключения корректны.
- Проверить загрузку страницы без ошибок JavaScript и необработанных ошибок в консоли.
- Проверить наличие всех обязательных областей, кнопок, полей периода и доступных подписей.
- Проверить расчёт текущей недели с понедельника по воскресенье, включая границы месяца и года.
- Проверить формат дат `ДД.ММ.ГГГГ` и ставок `1`, `0,5`, `0,25`.
- Проверить сортировку по дате и ФИО, а также включительность обеих границ периода.
- Проверить сохранение и восстановление периода и активного фильтра после перезагрузки.
- Проверить безопасный запуск с пустым `localStorage` и резервный режим при недоступности календарной зависимости.

### Ручная проверка пользователем

- Оценить, соответствует ли общий внешний вид современному минималистичному стилю и заданной палитре.
- Оценить, удобно ли расположены основные блоки и понятны ли две кнопки создания событий.

### История выполнения шага 1

**Дата завершения:** 19.08.2026.

**Реализовано:**

- созданы только три предусмотренных прикладных файла: `index.html`, `style.css`, `script.js`;
- собран семантический основной экран со всеми областями шага: шапкой, двумя кнопками создания, выбором периода, интерактивной сводкой, месячным календарём, таблицей, контейнерами модальных окон и уведомлений;
- добавлены точные справочники подразделений и должностей из ТЗ, типы событий и допустимые для них статусы;
- реализованы модель состояния, версия формата данных и ключи `hrPlannerData` и `hrPlannerSettings`;
- реализованы чтение и сохранение данных и настроек в `localStorage`, безопасное начальное состояние и восстановление корректных настроек;
- реализован период текущей недели с понедельника по воскресенье, проверка порядка дат, переход календаря к началу выбранного периода и сохранение периода;
- реализованы фильтры «Все / Приёмы / Увольнения», их сохранение и состояния `aria-pressed`;
- реализованы русское форматирование дат и ставок, включительная проверка периода и сортировка по дате и ФИО;
- подключён FullCalendar через CDN: месячная сетка, русская локаль, начало недели с понедельника и навигация по месяцам;
- реализован резервный экран при недоступности FullCalendar; таблица и данные при этом остаются доступны;
- создано базовое адаптивное тёмное оформление в чёрной, белой и мятной палитре, включая контрольные мобильные стили и `prefers-reduced-motion`;
- формы и CRUD намеренно не реализованы, поскольку относятся к следующим шагам.

**Автоматические проверки:**

- `script.js` успешно прошёл синтаксическую проверку комплектным Node.js (`exit code 0`);
- пройдено 9 модульных проверок: текущая неделя, границы месяца и года, формат даты, три формата ставки, включительность границ периода, исключение даты вне периода и сортировка;
- пройдено 7 проверок хранилища: пустой запуск, повреждённый JSON, неизвестная версия, сохранение данных, сброс ошибочного диапазона, восстановление и сохранение настроек;
- статически проверены 21 уникальный HTML-идентификатор, наличие всех обязательных областей и подключений, отсутствие дублирующихся `id`, баланс фигурных скобок CSS и наличие мобильного и reduced-motion режимов;
- подтверждено, что в рабочей папке присутствуют три прикладных файла и три документа проекта: `concept.md`, `tz.md`, `plan.md`.

**Нюансы и ограничения проверки:**

- встроенный браузер автоматизации не запустился из-за системного запрета среды на чтение `C:\Users\lalae\AppData` (`EPERM`) до открытия страницы; поэтому реальный браузерный прогон DOM, кликов и перезагрузки в этом шаге недоступен;
- загрузка FullCalendar с CDN и визуальный резервный режим проверены по структуре и логике кода, но не исполнены в браузере по указанной причине;
- полная обработка повреждённого хранилища с пользовательским окном предусмотрена шагом 4; на текущем шаге повреждённые данные безопасно заменяются пустым состоянием в памяти и не перезаписываются автоматически.

**Добавления сверх плана:** отсутствуют.

## Шаг 2. Создание записей и вывод данных — ✅ выполнен

### Работа

- Реализовать отдельные модальные формы «Запланировать приём» и «Запланировать увольнение».
- Добавить все поля из ТЗ, автоматический начальный статус и неизменяемые справочники.
- Реализовать создание события из основной кнопки и из выбранной даты календаря.
- Реализовать всю валидацию: обязательность, нормализацию ФИО, диапазон ставки, ввод ставки с запятой или точкой, запрет прошедшей даты и точных дубликатов.
- Безопасно сохранять новую запись в `localStorage` и показывать уведомление об успехе.
- Вывести записи в месячном календаре, таблице и сводных счётчиках.
- Реализовать навигацию календаря, отображение первых трёх событий дня и окно «Ещё N».
- Реализовать кликабельные сводные карточки, фильтр «Все / Приёмы / Увольнения» и пустое состояние таблицы согласно ТЗ.

### Готовый результат

Кадровик может создать корректный приём или увольнение любым предусмотренным способом. Запись сразу сохраняется и согласованно появляется в календаре, таблице и сводке. Некорректные записи и точные дубликаты не сохраняются.

### Автоматическая проверка

- Проверить создание приёма и увольнения с правильным начальным статусом.
- Проверить создание события нажатием на дату календаря и автоматическую подстановку даты.
- Проверить все обязательные поля и перевод фокуса на первое ошибочное поле.
- Проверить даты: прошлая запрещена, текущая и будущая разрешены.
- Проверить ставки на границах `0,01` и `1`, ввод через запятую и точку, а также отклонение пустых, нулевых, отрицательных и превышающих `1` значений.
- Проверить нормализацию ФИО и блокировку дубликатов с отличиями в регистре, пробелах и `Е/Ё`.
- Проверить, что разные типы или разные даты для одного сотрудника разрешены.
- Проверить сохранение после перезагрузки и отсутствие HTML-инъекции через ФИО и другие поля.
- Проверить соответствие календаря и таблицы данным, сортировку, счётчики, фильтры и более трёх событий в один день.
- Проверить, что навигация по месяцам не меняет период таблицы и сводки.

### Ручная проверка пользователем

- Оценить удобство заполнения формы и понятность сообщений об ошибках.
- Оценить читаемость событий в календарной ячейке и удобство просмотра дня с большим числом событий.
- Оценить, достаточно ли наглядно различаются приёмы, увольнения и отменённые события.

### История выполнения шага 2

**Дата завершения:** 19.08.2026.

**Реализовано:**

- добавлены отдельные точки входа для создания приёма и увольнения с общей модальной формой и корректным заголовком;
- в форме реализовано переключение типа события, динамическая подпись даты и автоматический нередактируемый начальный статус `Прием` или `Увольнение`;
- добавлены обязательные поля ФИО, должности, подразделения, даты события и ставки; справочники доступны только для выбора;
- комментарий при создании не выводится и сохраняется пустой строкой;
- реализовано создание события из пустой даты календаря через выбор «Приём / Увольнение» с подстановкой даты;
- реализованы нормализация ФИО, требование хотя бы одной буквы, проверка обязательных полей и справочников;
- реализован ввод ставки с точкой или запятой и диапазон от `0,01` до `1` включительно;
- текущая и будущие даты разрешены, прошедшие даты запрещены;
- точный дубликат по нормализованному ФИО, типу и дате блокируется; сравнение игнорирует регистр, повторные пробелы и `Е/Ё`;
- запись получает уникальный идентификатор, начальный статус, пустой комментарий и технические даты, после чего сохраняется в `localStorage`;
- состояние приложения изменяется только после успешной записи в `localStorage`; при ошибке выводится уведомление без ложного успеха;
- добавлены уведомления об успешном сохранении и ошибке хранилища;
- таблица выводит все предусмотренные поля, сортирует записи по дате и ФИО и остаётся с пустым телом при отсутствии записей;
- календарь выводит ФИО, тип и статус, различает приём и увольнение, приглушает отменённые события, ограничивает день тремя событиями и открывает список по «Ещё N»;
- добавлен отдельный CDN-файл русской локали FullCalendar, чтобы русские названия не зависели от состава основного пакета;
- сводные карточки считают все события своего типа в выбранном периоде и фильтруют таблицу и календарь; повторное нажатие снимает фильтр;
- пользовательские значения в таблице, календарном списке и уведомлениях выводятся через безопасные DOM-свойства `textContent`;
- добавлены стили форм, ошибок, модальных окон, уведомлений, меток типов и статусов, списка дня и мобильных карточек таблицы.

**Автоматические проверки:**

- `script.js` успешно прошёл синтаксическую проверку комплектным Node.js (`exit code 0`);
- пройдено 27 тестов валидации и создания: нормализация ФИО, `Е/Ё`, ставки с запятой и точкой, границы ставки, ошибочные ставки, обязательные поля, наличие буквы в ФИО, текущая/будущая/прошлая даты, точные дубликаты, допустимые разные типы и даты, форма созданной записи, запись в хранилище, отсутствие повторной записи и откат при ошибке хранилища;
- пройдено 9 регрессионных тестов шага 1: текущая неделя, граница года, русские форматы, включительные границы периода, сортировка, пустое хранилище и восстановление настроек;
- статически проверены уникальность 21 постоянного HTML-идентификатора, наличие обязательных контролов, порядок подключения основного календаря, русской локали и `script.js`, баланс CSS и наличие обработчиков шага 2;
- проверено отсутствие прямой вставки объектов записей через `innerHTML`; динамические пользовательские строки выводятся безопасно.

**Нюансы и ограничения проверки:**

- реальный браузерный прогон кликов, рендера FullCalendar, перезагрузки и адаптивного вида снова недоступен из-за системного ограничения среды `EPERM` на `C:\Users\lalae\AppData`, зафиксированного на шаге 1;
- поэтому обработчики кнопок и календаря проверены по коду и тестам вызываемой ими бизнес-логики, но не исполнены автоматизацией в настоящем браузере;
- столбец «Действия» на этом шаге намеренно пуст, а события календаря и элементы списка дня пока не открывают карточку просмотра: просмотр, редактирование и остальные действия относятся к шагу 3;
- полная пользовательская обработка повреждённого хранилища остаётся в шаге 4.

**Добавления сверх плана:** отсутствуют. Отдельное подключение русской локали FullCalendar является технической детализацией требования ТЗ о русском интерфейсе, а не новой функцией.

## Шаг 3. Полный цикл работы с кадровой записью — ✅ выполнен

### Работа

- Реализовать отдельный режим просмотра всех полей записи.
- Реализовать редактирование, включая допустимую работу с прошедшей записью и подтверждаемую смену типа события.
- Реализовать предупреждение при закрытии формы с несохранёнными изменениями.
- Реализовать быструю смену статуса только в рамках типа события и ввод необязательного комментария.
- Реализовать дублирование с копированием полей, очисткой даты и комментария и сбросом статуса.
- Реализовать удаление с подтверждением и возможность отмены удаления в течение семи секунд.
- Обеспечить немедленное согласованное обновление календаря, таблицы, сводки и `localStorage` после каждой операции.

### Готовый результат

Для любой записи доступен полный предусмотренный ТЗ жизненный цикл: просмотр, редактирование, ручная фиксация результата, дублирование, удаление и восстановление после удаления. Все представления и сохранённые данные остаются согласованными.

### Автоматическая проверка

- Проверить открытие одной записи из календаря и таблицы и корректный вывод всех полей.
- Проверить редактирование каждого поля и сохранение результата после перезагрузки.
- Проверить разрешённые статусы отдельно для приёма и увольнения, обратные переходы и отсутствие автоматической смены статуса.
- Проверить открытие комментария при смене статуса, сохранение с комментарием и без него.
- Проверить подтверждение смены типа, смену подписи даты и сброс статуса.
- Проверить редактирование полей и статуса прошлой записи, запрет замены её даты на другую прошлую дату.
- Проверить предупреждение о несохранённых изменениях и отсутствие предупреждения без изменений.
- Проверить дублирование: новый `id`, пустая дата, начальный статус, пустой комментарий и отсутствие записи до сохранения формы.
- Проверить подтверждение удаления, исчезновение записи из всех представлений, восстановление с тем же `id` в течение семи секунд и завершение возможности восстановления по таймеру.
- Проверить сохранность данных после последовательности нескольких операций и перезагрузки страницы.

### Ручная проверка пользователем

- Оценить, удобно ли переходить между просмотром и редактированием.
- Оценить понятность быстрого изменения статуса, дублирования, подтверждения удаления и отмены удаления.

### История выполнения шага 3

**Дата завершения:** 19.08.2026.

**Реализовано:**

- добавлен отдельный режим просмотра записи без редактируемых полей со всеми пользовательскими данными;
- запись открывается из строки таблицы, кнопки «Просмотреть», события календаря и списка событий выбранного дня;
- в режиме просмотра доступны быстрый статус и кнопки «Изменить», «Дублировать», «Удалить», «Закрыть»;
- в таблицу добавлены четыре предусмотренных действия для каждой записи и компактный список быстрой смены статуса;
- реализована форма редактирования всех полей, включая статус и комментарий;
- при закрытии изменённой формы через кнопку, крестик, фон или `Escape` запрашивается подтверждение потери несохранённых изменений;
- форма без изменений закрывается без предупреждения;
- при смене типа события запрашивается подтверждение, подпись даты изменяется, а статус сбрасывается на начальный статус нового типа;
- отмена подтверждения смены типа восстанавливает прежний выбор;
- прошедшую запись разрешено редактировать, если её дата не меняется; заменить дату на другую прошедшую дату нельзя;
- реализована быстрая смена статуса только в рамках типа события, включая обратные переходы;
- для итоговых и проблемных статусов открывается компактная форма с вариантами «Сохранить», «Без комментария» и «Отмена»;
- автоматическая смена статуса отсутствует;
- реализовано дублирование: форма получает тип, ФИО, должность, подразделение и ставку, но дата и комментарий очищаются, а статус сбрасывается; запись не создаётся до сохранения;
- реализовано модальное системное подтверждение удаления с ФИО, типом и датой события;
- после удаления показывается уведомление с кнопкой «Отменить» на 7 секунд;
- отмена восстанавливает запись с тем же идентификатором и в исходной позиции; при следующем удалении предыдущая возможность отмены прекращается;
- операции обновления, смены статуса, удаления и восстановления сначала записывают новый набор в `localStorage` и только после успеха изменяют состояние приложения;
- после каждой успешной операции календарь, таблица и сводка обновляются согласованно;
- добавлены адаптивные стили режима просмотра, действий таблицы, статусов, опасной кнопки удаления и уведомления с действием.

**Автоматические проверки:**

- итоговый `script.js` успешно загружается комплектным Node.js без синтаксических ошибок;
- пройдено 14 тестов жизненного цикла: создание, редактирование полей/статуса/комментария, запрет чужого статуса, смена статуса, неизменённая прошедшая дата, запрет другой прошедшей даты, смена типа, удаление, восстановление с тем же `id`, запрет повторного восстановления и откат обновления, статуса и удаления при ошибке хранилища;
- пройдено 24 теста статусов и регрессии: все статусы обоих типов, обратные переходы к начальному статусу, повторная проверка создания, ставок, дат, нормализации ФИО и русских форматов;
- статически проверено наличие обработчиков просмотра, редактирования, дублирования, быстрой смены статуса, удаления, восстановления, клика календарного события и действий таблицы;
- статически подтверждены предупреждения о несохранённых изменениях и смене типа, продолжительность отмены удаления 7 секунд и подпись кнопки «Отменить»;
- проверен безопасный вывод значений режима просмотра через `textContent` и баланс CSS после добавления новых компонентов.

**Нюансы и ограничения проверки:**

- подтверждения закрытия изменённой формы, смены типа и удаления реализованы нативными модальными диалогами браузера `window.confirm`; это соответствует требованию явного подтверждения и не добавляет внешних зависимостей;
- реальный браузерный прогон кликов, фокуса, модальных переходов и семисекундного таймера недоступен из-за прежнего ограничения среды `EPERM` на системную папку профиля;
- логика всех операций и откатов проверена напрямую, а наличие и связывание браузерных обработчиков проверено статически;
- полное удержание фокуса внутри модального окна и расширенная клавиатурная проверка относятся к шагу 4.

**Добавления сверх плана:** отсутствуют.

## Шаг 4. Устойчивость, адаптивность и доступность — ✅ выполнен

### Работа

- Реализовать обработку повреждённого JSON, некорректных отдельных записей, неизвестной версии данных, запрета или переполнения `localStorage`.
- Реализовать безопасное согласование данных между вкладками через событие `storage`, включая защиту несохранённой формы.
- Завершить адаптивную вёрстку для ширины от `320 px`: преобразование строк таблицы в карточки, адаптацию календаря, форм и модальных окон.
- Завершить клавиатурное управление, удержание и возврат фокуса в модальных окнах, доступные подписи, сообщения ошибок и уведомления.
- Проверить контраст, размеры областей нажатия, масштабирование и режим сокращённой анимации.

### Готовый результат

Приложение устойчиво к предусмотренным ошибкам браузерного хранилища и изменениям в другой вкладке, не теряет данные молча и полноценно работает на компьютере и телефоне, с мышью, сенсорным экраном и клавиатурой.

### Автоматическая проверка

- Подставить повреждённый JSON и проверить окно восстановления без автоматического удаления данных.
- Подставить смешанный набор корректных и некорректных записей и проверить загрузку корректной части.
- Имитировать ошибку записи и убедиться, что операция откатывается и не показывается как успешная.
- Проверить обновление данных между двумя вкладками и защиту открытой изменённой формы.
- Проверить отсутствие горизонтальной прокрутки страницы на ширинах `320`, `375`, `768` и `1200 px`.
- Проверить преобразование таблицы в карточки до `768 px` и наличие всех полей и действий.
- Проверить управление через `Tab`, `Shift+Tab`, `Enter`, `Space` и `Escape`, удержание фокуса и его возврат.
- Проверить наличие связанных `label`, ARIA-атрибутов, `aria-live`, видимого фокуса и семантических элементов.
- Проверить минимальные области нажатия, контраст WCAG AA доступными инструментами и масштабирование до 200%.
- Повторно проверить отсутствие небезопасного вывода пользовательского HTML и необработанных ошибок консоли.

### Ручная проверка пользователем

- Оценить читаемость и удобство интерфейса на реальном телефоне и компьютере.
- Оценить удобство мобильных карточек, календаря, модальных окон и областей нажатия.
- Подтвердить, что контраст, размеры текста и визуальный фокус комфортны при обычном использовании.

### История выполнения шага 4

**Дата завершения:** 19.08.2026.

**Реализовано:**

- добавлена строгая проверка корневого формата, версии и каждой записи при чтении `localStorage`;
- корректные записи загружаются, отдельные некорректные записи и повторяющиеся идентификаторы пропускаются с пользовательским предупреждением;
- сохранённые должности и подразделения вне текущих справочников не делают запись нечитаемой: такую запись можно просмотреть, а при редактировании потребуется выбрать допустимое значение;
- повреждённый JSON, неизвестная версия и некорректный корневой объект не перезаписываются автоматически;
- при повреждённых данных открывается обязательное окно с действиями «Повторить загрузку» и «Очистить и продолжить»;
- очистка повреждённых данных требует отдельного подтверждения и выполняется только по явному действию кадровика;
- отдельно обрабатывается полный запрет доступа к `localStorage`: пользователю предлагается повторить загрузку без недоступной операции очистки;
- ошибки сохранения записей по-прежнему откатывают операцию в памяти, а ошибки сохранения настроек теперь выводятся пользователю;
- добавлена обработка события `storage`: изменения записей и настроек из другой вкладки обновляют все представления;
- если в текущей вкладке изменена форма, внешнее обновление откладывается, пользователь получает предупреждение, а сохранение устаревшей формы блокируется;
- после закрытия такой формы актуальные внешние данные применяются автоматически; повреждённое внешнее содержимое направляется в окно восстановления;
- реализовано удержание фокуса внутри модального окна для `Tab` и `Shift+Tab`, закрытие через `Escape` с учётом несохранённых изменений и возврат фокуса к исходному элементу;
- переходы между вложенными состояниями просмотра, редактирования и статуса сохраняют исходный внешний элемент для возврата фокуса;
- подтверждены и дополнены семантические роли диалогов, связанные заголовки, подписи полей, `aria-describedby`, `aria-invalid`, `aria-live` и видимый фокус;
- минимальная область нажатия действий таблицы, статуса и действия уведомления увеличена до `44 px`;
- для страницы запрещён случайный горизонтальный выход за экран, календарные события на телефоне сокращаются с многоточием;
- сохранены мобильные карточки таблицы, одноколоночные формы и подробности, адаптивные модальные окна и режим `prefers-reduced-motion`.

**Автоматические проверки:**

- итоговый `script.js` успешно прошёл синтаксическую проверку комплектным Node.js;
- пройдено 9 тестов устойчивости хранилища: пустое состояние, повреждённый JSON, неизвестная версия, неверный корневой объект, смешанный набор записей, повторяющиеся `id`, чтение старого значения справочника, отсутствие автоматической перезаписи повреждённых данных, полный запрет чтения и ошибка сохранения настроек;
- пройдено 16 регрессионных тестов жизненного цикла: создание двух типов, все разрешённые статусы, редактирование, удаление, восстановление, базовая валидация и транзакционный откат при ошибке записи;
- статически пройдено 13 проверок: viewport, русский язык документа, `aria-live`, баланс CSS, минимальная ширина 320 px, мобильные карточки, области нажатия 44 px, сокращённая анимация, видимый фокус, ARIA диалогов, ловушка фокуса, обработчик `storage` и окно повреждённого хранилища;
- рассчитан контраст основных сочетаний: белый на чёрном — `18,82:1`, мятный на чёрном — `12,77:1`, чёрный на мятном — `12,39:1`; все значения превышают требование WCAG AA `4,5:1`;
- повторно проверен баланс фигурных скобок CSS и отсутствие синтаксических ошибок JavaScript.

**Нюансы и ограничения проверки:**

- реальный браузерный прогон двух вкладок, клавиатурного фокуса, экранного диктора и адаптивных размеров недоступен из-за ограничения среды `EPERM` на системную папку профиля;
- синхронизация вкладок проверена на уровне парсера, состояния и статического связывания обработчика; её фактическое поведение в двух окнах остаётся ручной браузерной проверкой;
- отсутствие горизонтальной прокрутки, удобство при масштабе 200% и реальные размеры областей нажатия проверены по CSS-правилам, но требуют визуального подтверждения в браузере;
- автоматический расчёт контраста охватывает основные цветовые пары; субъективную читаемость вторичных текстов и календаря должен подтвердить пользователь.

**Добавления сверх плана:** отсутствуют.

## Шаг 5. Итоговая приёмочная проверка — ✅ выполнен

### Работа

- Последовательно пройти все 21 сценарий использования и 28 ручных проверок, перечисленных в `tz.md`, автоматизировав браузерные действия везде, где результат можно определить объективно.
- Проверить критерии приёмки и отсутствие функций, исключённых из объёма работ.
- Исправить найденные расхождения и повторить затронутые проверки.
- Выполнить финальную проверку проекта с чистым `localStorage` и с заранее подготовленным набором событий.

### Готовый результат

Готовое к передаче приложение, соответствующее `tz.md`, с подтверждённой работой всех функций, сохранения данных, валидации, кнопок, сценариев и состояний ошибок.

### Автоматическая проверка

- Запустить полный сквозной набор проверок в актуальном Chromium: первый запуск, CRUD, статусы, комментарии, периоды, фильтры, календарь, дубликаты, прошлые даты, сохранение, две вкладки и ошибки хранилища.
- Проверить работу при прямом открытии `index.html` без локального сервера.
- Провести проверку консоли на необработанные ошибки на каждом основном сценарии.
- Проверить DOM на дублирующиеся идентификаторы, отсутствующие подписи полей и недоступные кнопки.
- Сопоставить реализованные элементы и поведение со всеми критериями приёмки из `tz.md`.
- Зафиксировать результаты: какие проверки выполнены автоматически, их результат и известные ограничения среды проверки.

### Ручная проверка пользователем

- Провести финальную визуальную приёмку на компьютере и телефоне: внешний вид, понятность, читаемость и удобство выполнения основных кадровых операций.
- Сообщить только о субъективных замечаниях к оформлению или удобству; функциональные проверки выполняет разработчик.

### История выполнения шага 5

**Дата завершения:** 19.08.2026.

**Выполнено:**

- реализация повторно сопоставлена с 21 сценарием использования, 28 ручными проверками и критериями приёмки из `tz.md`;
- повторно проверен полный цикл приёма и увольнения: создание, вывод, фильтрация, просмотр, редактирование, статусы, комментарий, дублирование, удаление и восстановление;
- повторно проверены даты, ставки, точные дубликаты, русское форматирование, справочники, сортировка, период и календарная выборка;
- проверена сериализация готовых записей в формат версии 1 и повторное чтение сохранённого набора;
- повторно проверены повреждённые данные, неизвестная версия, откат при ошибке квоты и отсутствие серверных запросов для пользовательских данных;
- проверено наличие всех обязательных областей, столбцов, функций, адаптивных режимов и атрибутов доступности;
- подтверждено отсутствие функций, исключённых из объёма работ: авторизации, сервера, облачной синхронизации, импорта/экспорта, прогноза, поиска, редактирования справочников, светлой темы и демонстрационных данных;
- проверено отсутствие `TODO`/`FIXME`, дублирующихся постоянных HTML-идентификаторов и несбалансированных CSS-блоков;
- итоговый JavaScript повторно прошёл синтаксическую проверку без ошибок;
- найден и исправлен пограничный дефект отмены быстрой смены статуса: при закрытии окна комментария крестиком, фоном или клавишей `Escape` табличный список теперь возвращается к фактически сохранённому статусу;
- после исправления повторно выполнены сквозные и статические проверки.

**Автоматические проверки:**

- пройдено 37 сквозных утверждений логики: четыре исходных события, уникальные идентификаторы и технические даты, включительные границы периода, сортировка, независимая календарная выборка, оба фильтра, точные дубликаты, разные типы одного сотрудника, редактирование, все статусы, запрет статуса чужого типа, работа с прошлой записью, удаление/восстановление, сериализация/чтение, повреждённый JSON, неизвестная версия, транзакционные откаты, граница недели на стыке года, русские форматы и ошибочные ставки;
- пройдено 13 итоговых проверок структуры: наличие трёх прикладных файлов и локальных связей, уникальные `id`, обязательные области, все столбцы таблицы, баланс CSS, отсутствие незавершённых пометок, отсутствие серверных вызовов, безопасный вывод пользовательских строк, все обязательные операции, русская локаль календаря, адаптивность и доступность;
- проверено отсутствие синтаксических ошибок в итоговом `script.js`;
- результаты шага 4 по хранилищу, контрасту WCAG AA и адаптивным правилам сохранены и не регрессировали;
- все выполненные проверки завершились успешно; необработанных ошибок в доступном JavaScript-runtime нет.

**Нюансы и ограничения проверки:**

- полноценный запуск `index.html` в реальном браузере, автоматические клики, DOM-рендер FullCalendar и проверка консоли страницы недоступны из-за системного ограничения среды `EPERM` на `C:\Users\lalae\AppData`;
- по этой причине визуальная проверка ширин 320/768/1200 px, масштаба 200%, двух реальных вкладок, ловушки фокуса и таймера уведомления остаётся пользователю;
- загрузка CDN FullCalendar и русской локали проверена по корректности URL, порядку подключения и логике резервного состояния, но требует фактического запуска при доступном интернете;
- объективная бизнес-логика, формат хранилища и транзакционные операции проверены напрямую без браузера.

**Добавления сверх плана:** существующий диагностический объект `window.hrPlanner` расширен разделом `queries` с функциями выборки таблицы и календаря. Это не добавляет пользовательской функции и не меняет интерфейс; расширение использовано только для автоматической итоговой проверки фильтров, периода и независимой календарной выборки.

### Исправление после приёмки

**Дата:** 19.08.2026.

- по результату ручного запуска обнаружено, что кнопка «Сохранить» визуально находилась в нижней панели модального окна, но технически была расположена вне элемента `<form>` и поэтому не вызывала событие отправки;
- форме присваивается уникальный `id`, а кнопка «Сохранить» явно связывается с ней стандартным атрибутом `form`;
- исправление применяется к созданию, редактированию и дублированию, поскольку эти режимы используют общую форму;
- добавлена статическая регрессионная проверка связи кнопки с формой;
- новых пользовательских функций не добавлено.

## Итоговый принцип выполнения

Следующий шаг начинается только после того, как результат текущего шага работает целиком и прошёл относящиеся к нему автоматические проверки. Обнаруженные дефекты исправляются в том же шаге, а не переносятся на финальную приёмку. Пользователю не передаются для ручной проверки логика, кнопки, сохранение, валидация или иные объективно проверяемые функции.
